name: openvb
description: An implementation of the OpenVBconf standard in Dart.
version: 1.0.0
repository: https://github.com/ExpressiveLabs/openvb-dart

environment:
  sdk: ^3.5.3

# Add regular dependencies here.
dependencies:
  # native_toolchain_rust: ^0.1.0-dev.7
  # native_assets_cli: ^0.7.3
  # ffi: ^2.1.3
  json_annotation: ^4.9.0

dev_dependencies:
  # build_runner: ^2.4.13
  json_serializable: ^6.9.0
  lints: ^4.0.0
  test: ^1.24.0
